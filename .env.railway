# Railway Environment Variables Template
# Copy these to your Railway project settings

# Required for production
NODE_ENV=production

# Port (Railway provides this automatically, but good to document)
# PORT=8080

# Your API keys (set these in Railway dashboard as secrets)
# ODDS_API_KEY=your-odds-api-key-here

# Database (if using Railway Postgres)
# DATABASE_URL will be automatically provided by Railway if you provision a database
